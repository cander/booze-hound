<!doctype html>
<html lang="en">
<head>
  <%= render 'layouts/components/head' %>
</head>
<body>
  <div class="container my-4">
    <!-- BEGIN page container -->
    <div class="my-2 p-2 border border-primary-subtle">
      Simulate user experience:</a>
      <input type="radio" class="btn-check" name="auth-options" id="anon-toggle" autocomplete="off">
      <label class="btn btn-outline-success btn-sm" for="anon-toggle">Anonymous</label>
      <input type="radio" class="btn-check" name="auth-options" id="auth-toggle" autocomplete="off" checked>
      <label class="btn btn-outline-danger btn-sm" for="auth-toggle">Authenticated</label>
      <div class="mt-3">
        <div class="border border-success p-2 my-2"><span class="text-success fw-bold pe-1 border-end border-success border-3">ANONYMOUS</span> Sections with this border color (<span class="text-success">green</span>) are for anonymous users.</div>
        <div class="border border-danger p-2 my-2"><span class="text-danger fw-bold pe-1 border-end border-danger border-3">AUTHENTICATED</span> Sections with this border color (<span class="text-danger">red</span>) are dynamic and will need to be moved behind an authentication check.</div>
      </div>
    </div>
    <%= render 'layouts/components/header' %>
    <main>
      <%= yield %>
    </main>
    <!-- END Body include -->
    <%= render 'layouts/components/footer' %>
  </div><!-- END page container -->
  <%= render 'layouts/components/foot' %>
  <%= javascript_include_tag 'application'%>
</body>
</html>
