<%= stylesheet_link_tag "tables" %>


<h2><%= @store.name %><br></h2>
<%= @store.address %><br>
<%= @store.store_hours %><br>

<h2>
Inventory
</h2>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Item Code</th>
      <th>Quantity</th>
    </tr>
  </thead>
  <tbody>
  <% @inventory.each_with_index do |inv, index| %>
      <tr class="<%= index.even? ? 'even-row' : 'odd-row' %>">
        <td><%= inv.olcc_bottle.name %></td>
        <td><%= inv.olcc_bottle.new_item_code %></td>
        <td><%= inv.quantity %></td>
      </tr>
    <% end %>
  </tbody>
</table>
