<!-- BEGIN Home include -->
<% if user_signed_in? %>
  <div class="row">
    <div class="col">
      <div class="alert alert-primary" role="alert">
        <p class="m-0 p-0"><i class="fa-regular fa-message me-2 fs-4 align-middle"></i>Welcome, <%= @user.first_name %>!</p>
      </div>
    </div>
  </div>
<% end %>
<div class="row">
  <div class="col-lg-4 order-lg-2">
    <% if user_signed_in? %>
      <%= render 'layouts/sidebar/user_menu_authenticated' %>
    <% else %>
      <%= render 'layouts/sidebar/home_anonymous' %>
    <% end %>
  </div>
  <div class="col-lg-8 order-lg-1">
    <!-- BEGIN Main Body Content include -->
    <% if user_signed_in? %>
      <%= render 'layouts/content/home_authenticated' %>
    <% else %>
      <%= render 'layouts/content/home_anonymous' %>
    <% end %>
    <!-- END Main Body Content include -->
  </div>
</div>
<div class="mt-5 border border-1 p-3">
<h5><em>Warning: some hacky UI stuff below:</em></h5>
<% if user_signed_in? %>
  <p><em>Hacky admin function I don't want to lose:</em></p>
  <p>Add a bottle based on the OLCC new item code:</p>
  <%= form_with url: olcc_bottles_path do |form| %>
    <div>
      <%= form.label :new_item_code %>
      <%= form.text_field :new_item_code %>
      <%= category_select(form) %>
    </div>
    <div>
      <%= form.submit "Search" %>
    </div>
  <% end %>
<% else %>
  <%= link_to "Login or signup page", new_user_session_path %>
<% end %>
</div>
